<!DOCTYPE html>
		<div class="row">
          <div class="col-xs-12 col-sm-8 col-md-8 col-lg-8 col-lg-offset-2 col-md-offset-2 col-sm-offset-2">
            <div class="well">
            	<%= form_for @tag, html: {:class => "form-horizontal" } do |f| %>
				  <% if @tag.errors.any? %>
				    <div id="error_explanation">
				      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>
				
				      <ul>
				      <% @tag.errors.full_messages.each do |msg| %>
				        <li><%= msg %></li>
				      <% end %>
				      </ul>
				    </div>
				  <% end %>
            	
                <fieldset>
                  <legend><span class="glyphicon glyphicon-tag" style="vertical-align:left"></span> Tag</legend>
                  <div class="form-group">
                  	<%= f.label :tagname, "Tag Name:", :class => "col-lg-3 control-label" %>     
                    <div class="col-lg-7">
                      <%= f.text_field :tagname, :class => "form-control" %>       
                       Use single entry or comma separated values. e.g. VMware, Microsoft, London, Network, Security          	 
                    </div>
                  </div>                                                
                  <br>
                  <center>
	                  <div class="form-group">	                    
	                      <%= f.submit class: 'btn btn-primary' %>
	                  </div>
	                  <%= link_to tags_path, class: "pull-left", :method => :get do %>
	        			<span class="glyphicon glyphicon-arrow-left" rel="tooltip" title="Back" style="vertical-align:left"></span> Back
	    			  <% end %>
	                     
	                  <%= link_to :userroot, class: "pull-right", :method => :get do %>
	        			Home <span class="glyphicon glyphicon-home" rel="tooltip" title="Home" style="vertical-align:left"></span>
	    			  <% end %>
                  </center>	 
                </fieldset>
              
            </div>
          </div>
        </div>
<% end %>

</html>


